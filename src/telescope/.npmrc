# Supply-chain defense: require packages to be at least 5 days old before
# install. Mirrors the components-repo root `.npmrc`; this standalone UI
# package is excluded from the root pnpm workspace so it needs its own.
min-release-age=5
